#!/bin/bash
cp -r /workspace/solution/src/. /workspace/exercise/src/
cd /workspace/exercise && gradle compileKotlin -q
echo "Solution applied. Review FulfillmentActivitiesImpl.kt and FulfillmentWorkflowImpl.kt."
