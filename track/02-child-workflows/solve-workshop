#!/bin/bash
cp -r /workspace/solution/src/. /workspace/exercise/src/
cd /workspace/exercise && mvn compile -q
echo "Solution applied. Review InventoryReservationWorkflowImpl.java and FulfillmentWorkflowImpl.java."
