#!/bin/bash
cp -r /workspace/solution/src/. /workspace/exercise/src/
cd /workspace/exercise && mvn compile -q
echo "Solution applied. Review FulfillmentWorkflowImpl.java — note LocalActivityOptions and newLocalActivityStub."
